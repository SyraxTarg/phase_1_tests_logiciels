name: Lauch tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  hello_job:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "hello world"
